# Gradle
.gradle/
build/
**/build/
**/.gradle/

# Loom / NeoForge generated run + caches
run/
**/run/
*.classpath
*.project
bin/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.factorypath

# OS
.DS_Store
Thumbs.db
desktop.ini

# Gradle wrapper jar is kept intentionally (needed to build):
!gradle/wrapper/gradle-wrapper.jar

mappings/
intermediary/

# Staging directory for a release build (tools/build_release.sh)
build-release/

# iCloud Drive syncs ~/Desktop, where this repository lives, and resolves a sync conflict by leaving a
# copy such as "build 2.gradle" or "farlandsreforged 2/" beside the original. One commit swept 42 of
# them in, including three .java copies that stop the Fabric 26.x projects compiling from a clean clone.
* [0-9]
* [0-9].*
* [0-9][0-9]
* [0-9][0-9].*
